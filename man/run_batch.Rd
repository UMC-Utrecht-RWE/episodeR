% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_batch.R
\name{run_batch}
\alias{run_batch}
\title{Process Batches of Persons into Multivariate Episodes}
\usage{
run_batch(
  ids_subset,
  connection,
  sql_explosion,
  sql_combine,
  sql_mergestatus,
  study_variables,
  data_type_col = "data_type"
)
}
\arguments{
\item{ids_subset}{Vector of person_ids to process in this batch.}

\item{connection}{DBI connection used to execute SQL pipeline steps.}

\item{sql_explosion}{Loaded SQL (via picard::load_sql_query) for step 1.}

\item{sql_combine}{Loaded SQL for step 2.}

\item{sql_mergestatus}{Loaded SQL for step 3.}

\item{study_variables}{Data frame with variable metadata, used for
data-type conversion of the pivoted wide table.}

\item{data_type_col}{Name of the column in study_variables declaring
the target data type per variable, or NULL to skip conversion.}
}
\value{
data.table of merged multivariate episodes for ids_subset,
with columns person_id, start_episode, end_episode, and one column
per study variable.
}
\description{
Runs the three-step multivariate episode SQL pipeline (explosion,
combine, merge-status) for a single batch of person_ids, pivots the
result to wide format, applies declared data types, and encodes
episodes against a compact combination dictionary to keep the merge
step cheap.
}
\details{
Isolated from multivariate_episodes_pipeline() so it has no implicit
dependence on enclosing-scope state: every input it needs is an
explicit argument, and every DB side effect it produces (temp tables
it writes/drops) is documented below.

Side effects on \code{connection}: writes/overwrites temp table
\code{i_batch_persons}; reads/drops tables \code{multivariate_episode} and
\code{dim_var} produced by the SQL steps as a byproduct; writes
\code{multivariate_episode_coded} (overwritten, left in place for
sql_mergestatus to read against). Caller is responsible for any
cleanup of \code{multivariate_episode_coded} across batches if desired.
}
